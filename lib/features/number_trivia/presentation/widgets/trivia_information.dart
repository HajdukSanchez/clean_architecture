import 'package:flutter/material.dart';

class TriviaInformation extends StatelessWidget {
  const TriviaInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(
      children: const [
        Text("1234"),
        Text("lksjkasdasdasjd"),
      ],
    );
  }
}
